You are in repair + verification mode.
Assume EVERYTHING is broken until proven otherwise.

RULES

DO NOT finish early.

DO NOT skip steps.

DO NOT claim success without proof.

NO bulk file dumps.

All fixes must be real code changes.

PHASE 0 – GIT SANITY (BLOCKER)

Run:

git status


If uncommitted changes exist:

Create commit:

chore: checkpoint before asset remediation


Then continue.

If you skip this phase, the task FAILS.

PHASE 1 – ASSET FAILURE REPAIR
Problems to fix

Missing chip images (KNXT4)

Missing symbol files (BiO-Wheel)

Missing assets in BiO-Slotz

Orphaned files not referenced anywhere

Actions

Re-run asset audit script

For EVERY missing asset:

Identify:

Which screen uses it

Expected filename

Fix by ONE of:

Move existing file

Rename file

Update code reference

Log each fix in:

reports/asset_fixes.md


For orphaned assets:

Either:

Remove file

Or wire it into code

Explain decision.

FAIL if ANY missing asset remains.

PHASE 2 – SCREEN FAILURES (MANDATORY)

You MUST fix these failed screens:

Screen	Required Fix
KNXT4	Missing chips + layout
BiO-Wheel	Missing symbols
BiO-Lobby	Missing background
BiO-Slotz	Missing icons/assets
For EACH screen:

Open its HTML

List:

All assets it loads

Verify:

File exists

Correct path

Correct case

Fix:

Broken src paths

Wrong filenames

Directory mistakes

Log results in:

reports/screen_fixes.md


Each screen must end with:
PASS or FAIL.

PHASE 3 – BRIDGE LAYER HARDENING

Based on suspected failures:

Audit BioGameBridge.java

Confirm:

All methods annotated

No dead methods

No mismatches

Add runtime logging:

Log.d("Bridge", "Method called: playBioWheel");


In HTML:

Wrap calls in try/catch

try { BioGameJS.playBioWheel(); }
catch(e){ console.error(e); }


Create:

reports/bridge_audit.md

PHASE 4 – WEBVIEW RELIABILITY

Confirm in MainActivity:

JS enabled

DOM storage enabled

File access allowed

WebChromeClient set

Add:

webView.setWebContentsDebuggingEnabled(true);


Log any change.

PHASE 5 – FULL REVALIDATION

Re-test ALL screens:

Bio-Store

BiO-Petz

KNXT4

BiO-Wheel

BiO-Lobby

BiO-Slotz

Create:

reports/final_validation.md


For each:

Asset status

Navigation

PASS/FAIL

FAIL = auto-fix + retest.

PHASE 6 – DOCUMENTATION (NO EXCUSES)

Update:

docs/TASKS.md

Mark failed task

Mark new task completed

docs/WORKLOG.md

Timeline

Bugs found

Fixes applied

docs/DECISIONS.md

Why assets were missing

How structure was corrected

FINAL COMMIT

ONLY when all pass:

feat: fix asset failures + screen validation

ACCEPTANCE CONDITIONS

You may NOT finish unless:

Zero missing assets

All 6 screens PASS

Bridge calls verified

WebView stable

Git clean

Translation

This forces Kilo to:
• Clean git
• Fix real files
• Stop lying
• Re-test everything
• Document like a grown adult
